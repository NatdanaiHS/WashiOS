# Figure QA failure

The generated table and provenance passed, but visual inspection found the vertical colorbar label clipped at the right edge of `pair_condition_pattern.png`/`.pdf`. No manuscript used this render. Attempt r4 removes the unnecessary colorbar while preserving the pair-specific 3/3 cells and no-pooling label.
