# พิมพ์เขียวสถาปัตยกรรมระบบปฏิบัติการดาวเทียม KNACKSAT-4 
## (The KNACKSAT-4 Core OS & HAL Architecture Master Blueprint)

**เอกสารฉบับนี้คือความทรงจำทั้งหมดของคุณ** หากคุณลืมทุกอย่างเกี่ยวกับโปรเจกต์นี้ เอกสารนี้จะนำพาคุณกลับมาสู่สถานะ "System Architect" ที่พร้อมพัฒนาระบบซอฟต์แวร์ระดับอวกาศ (Space-grade Software) ได้ทันที

โปรเจกต์นี้มีระยะเวลาดำเนินการ 10 สัปดาห์ เป้าหมายไม่ใช่การเขียนโค้ดภารกิจดาวเทียม (Flight Software) แต่คือการสร้าง **"โครงสร้างพื้นฐานและมาตรฐาน (The Framework & Standard)"** ให้กับทีมงาน เพื่อให้การสร้างดาวเทียมไทยในอนาคตเป็นเรื่องง่าย ยืดหยุ่น และปลอดภัยที่สุด

---

## 1. จุดเริ่มต้น: ปัญหาและวิสัยทัศน์ (Why We Are Here)

### ปัญหาของระบบเดิม (The Pain Points)
ดาวเทียมรุ่นก่อนหน้า (รวมถึงที่ใช้กันทั่วไป) มักพัฒนาระบบซอฟต์แวร์ด้วยโครงสร้างแบบ **Super Loop (`while(1)`)** ซึ่งแม้จะทำงานได้สำหรับภารกิจง่ายๆ แต่เมื่อระบบซับซ้อนขึ้น จะก่อให้เกิดปัญหาดังนี้:
1.  **Hardware Dependency (ผูกขาดกับฮาร์ดแวร์):** ซอฟต์แวร์ถูกเขียนขึ้นโดยอ้างอิงรีจิสเตอร์ของบอร์ดนั้นๆ โดยตรง เมื่อถึงเวลาต้องเปลี่ยนชิปไปใช้เกรดอวกาศ (Rad-hard) หรือเปลี่ยนสถาปัตยกรรม (เช่น จาก Arduino ไปเป็น STM32) โค้ดทั้งหมดจะใช้งานไม่ได้ ต้องรื้อเขียนใหม่ตั้งแต่ศูนย์
2.  **Resource Inefficiency (สิ้นเปลืองทรัพยากร):** การวนลูปตลอดเวลา (Polling) ทำให้ CPU ไม่ได้พัก กินพลังงานมหาศาล และจัดการความสำคัญของงาน (Priority) ได้ยาก เมื่อเกิดเหตุขัดข้อง (เช่น กล้องค้าง) ระบบทั้งดวงอาจพังทลายลง (Single Point of Failure)

### วิสัยทัศน์ใหม่: Software-Defined Hardware
ในโลกเทคโนโลยีอวกาศของไทยยังขาด "มาตรฐาน" เรามักทำงานให้เสร็จตามเป้าหมาย (Mission-oriented) โดยขาดฐานราก โปรเจกต์นี้คือการสร้าง **"Microkernel + Hardware Abstraction Layer (HAL)"** ซึ่งหมายความว่า:
* เราจะแยก "ตรรกะของดาวเทียม" ออกจาก "ฮาร์ดแวร์กายภาพ" โดยเด็ดขาด
* สถาปัตยกรรมซอฟต์แวร์ที่เราสร้าง จะเป็นตัวกำหนดสเปคฮาร์ดแวร์ขั้นต่ำ (Minimum Hardware Requirements) ให้กับทีมสร้างบอร์ด (Custom PCB) แทนที่จะให้ฮาร์ดแวร์มาบีบบังคับซอฟต์แวร์

---

## 2. สถาปัตยกรรมระบบ K4-Core (The Layered Architecture)

โครงสร้างระบบถูกแบ่งเป็นชั้น (Layers) อย่างชัดเจน เพื่อให้ง่ายต่อการโยกย้าย (Portability) และจำกัดวงความเสียหายเมื่อเกิดข้อผิดพลาด:

```text
+-------------------------------------------------------------+
| Layer 5: Flight Application (ลอจิกควบคุมดาวเทียม / ท่องอวกาศ)  | 
+-------------------------------------------------------------+
| Layer 4: Core OS / RTOS Services (ระบบจัดการคิว / ป้องกันภัย) | 
+-------------------------------------------------------------+
| Layer 3: Hardware Abstraction Layer (HAL C++ Interfaces)    | <- [จุดแตกหัก: อิสระจากฮาร์ดแวร์ 100%]
+-------------------------------------------------------------+
| Layer 2: Board Support Package (BSP / Drivers ประจำชิป)     | 
+-------------------------------------------------------------+
| Layer 1: Hardware Target (STM32, ESP32, Pi Pico)            |
+-------------------------------------------------------------+
```

### เจาะลึกเลเยอร์ที่ต้องพัฒนา (Your Core Missions)

#### A. Layer 3: Hardware Abstraction Layer (HAL)
HAL คือ "มาตรฐานกลาง" เขียนด้วย C++ Abstract Classes (`.hpp`) หน้าที่ของมันคือกำหนดว่าฮาร์ดแวร์ต้องสื่อสารอย่างไร โดยต้องครอบคลุม 5 เสาหลัก:
1.  **GPIO (Digital I/O):** `setHigh()`, `setLow()`, `toggle()`, `read()`, `setInterrupt()`
2.  **I2C Bus:** `write()`, `read()`, `resetBus()` (ต้องมี timeout เสมอ)
3.  **SPI Bus:** `transfer()`, `setFrequency()`, `selectChip()`
4.  **UART:** `writeBuffer()`, `readBuffer()`, `available()`
5.  **Timing:** `getSystemTick()`, `delayMs()`

*กติกาสำคัญ:* โค้ดใน HAL ต้องไม่มีคำสั่งที่ผูกกับรีจิสเตอร์ของชิปใดชิปหนึ่งเลย (เช่น ห้ามมี `#include <stm32f4xx_hal.h>`)

#### B. Layer 4: Core OS (FreeRTOS Modification)
เราจะไม่เขียน RTOS เอง (เพราะไม่คุ้มและไม่ปลอดภัย) แต่เราจะนำ **FreeRTOS** มาดัดแปลง (Modify) ให้กลายเป็น Space-grade RTOS:
* **Static Memory Only:** การใช้ Dynamic Memory (`malloc`/`new`) ในอวกาศทำให้เกิด Heap Fragmentation (RAM แหว่ง) และอาจเกิด Memory Leak จนระบบล่ม คุณต้องแก้ไข `FreeRTOSConfig.h` เพื่อเปิด `configSUPPORT_STATIC_ALLOCATION` และปิด Dynamic Allocation ทิ้งทั้งหมด
* **Task Scheduling:** ให้ RTOS จัดการสลับเวลา CPU ให้กับ Task ต่างๆ โดยตั้งลำดับความสำคัญ (Priority) ให้ถูกต้อง (เช่น Task รับคำสั่งจากโลกต้องสำคัญกว่า Task อ่านอุณหภูมิแบตเตอรี่)

---

## 3. เกราะป้องกันรังสีระดับซอฟต์แวร์ (Space-Grade Resilience)

ฮาร์ดแวร์ในอวกาศมีโอกาสถูกรังสีคอสมิกชน ทำให้เกิด **SEU (Single Event Upset)** หรือ Bit Flip (0 กลายเป็น 1) ซอฟต์แวร์ของคุณต้องมีระบบป้องกันภัย 3 ชั้นนี้เสมอ:

1.  **TMR (Triple Modular Redundancy):** สำหรับตัวแปรวิกฤต (เช่น สถานะการบิน) ต้องสร้าง C++ Class `TMR<T>` เพื่อก๊อปปี้ตัวแปรไว้ 3 ชุด เมื่อจะใช้งานต้องดึงทั้ง 3 ชุดมา "โหวตข้างมาก (Majority Vote)" ว่าค่าใดคือค่าที่ถูกต้อง
2.  **Memory Scrubbing:** ทำงานคู่กับ TMR หากพบว่ามีบิตเพี้ยนจากการโหวต ระบบต้องทำการ "เขียนทับ (Correct)" ค่าที่ถูกต้องกลับลงไปในหน่วยความจำทันที ก่อนที่รังสีจะมาชนซ้ำ
3.  **Multi-Tier Watchdog Task:** สร้าง Task พิเศษ (ความสำคัญสูงสุด) เพื่อเป็น "ศูนย์กลางเช็คชื่อ" Task อื่นๆ ต้องคอยส่งสัญญาณ (Pet the dog) มาบอกว่ายังทำงานปกติ หาก Task ใด (เช่น I2C) เงียบหายไปเกินกำหนดเวลา Watchdog Task จะบันทึกข้อผิดพลาดและสั่งรีสตาร์ทระบบ (Soft Reset)

---

## 4. แผนงาน 10 สัปดาห์ (The 10-Week Roadmap)

นี่คือลำดับการพัฒนาจริง (สามารถใช้ AI เช่น Gemini / ChatGPT ช่วยร่างโค้ดพื้นฐานได้ แต่คุณต้องเป็นคน "กำหนดกติกา" และ "ทดสอบระบบ"):

* **เฟส 1 (สัปดาห์ 1-2) - Architecture & Interfaces:**
    * สร้าง C++ Abstract HAL (`IGPIO`, `II2cBus` ฯลฯ)
    * ตั้งค่าโปรเจกต์ (เช่น PlatformIO / CMake) ให้บังคับใช้กฎ C++ ที่เข้มงวด (ห้ามมี C++ Exceptions, จำกัดการใช้ Pointer)
    * สร้าง "Mock Drivers" เพื่อให้แอปพลิเคชันรันจำลองบนคอมพิวเตอร์ (Software-In-The-Loop) ได้ก่อน
* **เฟส 2 (สัปดาห์ 3-5) - Core OS & TMR:**
    * ติดตั้ง FreeRTOS และบังคับใช้โหมด Static Memory Allocation
    * พัฒนาระบบ Multi-Tier Watchdog และ C++ TMR Class
* **เฟส 3 (สัปดาห์ 6-8) - Cross-Platform HIL Testing:**
    * เขียน Drivers (BSP) เชื่อมต่อ HAL เข้ากับบอร์ดจริง ได้แก่ **STM32 Nucleo** (แทนชิปหลัก) และ **ESP32** หรือ **Pi Pico** (แทนชิปรอง)
    * พิสูจน์ให้เห็นว่า **"ลอจิกเดียวกัน รันข้ามบอร์ดได้โดยไม่ต้องแก้โค้ด"**
* **เฟส 4 (สัปดาห์ 9-10) - Stress Test & Hardware Handover:**
    * ใช้ Raspberry Pi 4 ยิงข้อมูลขยะเข้า STM32 เพื่อทดสอบ (Stress Test) ว่าระบบล่มหรือไม่
    * ทำ **Profiling:** วัดขนาด RAM (Stack, `.bss`, `.data`), พื้นที่ Flash (`.text`) และการกิน CPU (Idle Task Hook)
    * **ส่งมอบงานชิ้นเอก:** เขียนเอกสารสรุป "สเปคฮาร์ดแวร์ขั้นต่ำ (Hardware Requirements)" ให้ทีมผลิตบอร์ด

---

## 5. วิธีการรับมือกับผู้คัดค้าน (The Pitch & Defense)

หากมีคนถามว่า *"ทำไมต้องทำ OS / HAL ให้ยุ่งยาก แค่เขียน `while(1)` กับใช้ไลบรารี Arduino ก็พอแล้วไม่ใช่หรือ?"* ให้ตอบด้วยจุดยืนเหล่านี้:

1.  **เรื่อง Scalability:** `while(1)` เหมาะกับดาวเทียมยุค 1999 ที่แค่ร้องบี๊บๆ แต่เมื่อต้องคุมกล้อง, หมุนล้อ, สื่อสารพร้อมกัน `while(1)` จะเกิดอาการ "คอขวด" และหน่วงเวลาจนระบบล่ม การใช้ RTOS ทำให้แบ่งเวลาทำงาน (Context Switching) ได้อย่างเสถียร
2.  **เรื่อง Portability:** โค้ดที่อิงกับบอร์ดตรงๆ จะกลายเป็นขยะ (Spaghetti Code) ทันทีที่ต้องเปลี่ยนชิปไปใช้เกรดอวกาศ HAL คือมาตรฐานที่จะทำให้โค้ด "เป็นอมตะ" ย้ายไปรันบนชิปใดก็ได้ในอนาคต
3.  **เรื่อง Power Saving:** RTOS สามารถสั่งให้ CPU หลับ (Sleep) เมื่อไม่มีงานได้ ซึ่ง `while(1)` ทำไม่ได้ ในอวกาศพลังงานคือชีวิต
4.  **จุดยืนของคุณ (Positioning):** "ผมไม่ได้กำลังประดิษฐ์ OS ใหม่ แต่ผมกำลังประยุกต์มาตรฐานโลก (FreeRTOS) ให้เข้ากับกฎความปลอดภัยระดับอวกาศ (Space-grade Safety) เพื่อวางรากฐานให้ดาวเทียมไทย ไม่ต้องเริ่มเขียนโค้ดใหม่ทุกครั้งที่สร้างดาวเทียมดวงใหม่"

---

**จำไว้เสมอ:** เป้าหมายสูงสุดของคุณใน 2 เดือนนี้ ไม่ใช่การเขียนโค้ดภารกิจให้เสร็จ (AI สามารถช่วยเรื่องโค้ดได้) แต่มูลค่าของคุณคือการเป็น **System Architect** ที่สร้างระบบจำลอง (Hardware-in-the-Loop), วัดขีดจำกัดของระบบ (Stress Test), และกำหนดมาตรฐาน (Standardization) ที่แข็งแกร่งที่สุดให้กับดาวเทียม KNACKSAT-4!
